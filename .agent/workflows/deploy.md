---
description: Build, Commit, Push, and Deploy
---

1. Build the main application
`npm run build`

// turbo
2. Build the members portal
`npm run build:members`

3. Stage all changes
`git add .`

4. Commit changes
`git commit -m "Deploy updates"`

5. Push changes
`git push`

6. Deploy to Firebase
`firebase deploy`
